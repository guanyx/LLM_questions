Create a highly detailed explanatory infographic with accurate visual semantics, clear hierarchy, and Chinese labels where text appears inside the image:

**Title (top):**
"把70B的模型跑起来，需要多少成本？以DeepSeek-R1-70B为例"

**Main Content - Detailed sections:**
*Create one section for EACH core cost layer in the source text. Keep one continuous mini-case across the full page: the same DeepSeek-R1-Distill-Llama-70B handles the same long context strip labeled “128K token 上下文”, so the viewer can track the same object through baseline setup, weight loading, KV cache storage, prefill computation, and decode generation.*
*For each section, keep only 1 mandatory text block plus 2 non-redundant visual dimensions. Prefer larger labels and fewer micro-callouts over maximum detail density.*

**Section 1: 显存账之一：权重本体先抬高门槛**
- **Mandatory Text Block**: "对 70B 模型来说，第一笔账往往不是 KV cache，而是权重本体就已经很重。"
- Detailed diagram of one huge weight cabinet titled "70B 参数", split into three large stacked storage blocks labeled "fp16 / bf16 ≈ 140 GB", "fp8 ≈ 70 GB", "4 bit ≈ 35 GB"; place one consumer GPU silhouette card beside it labeled "RTX 5090 32GB", with a clear scale mismatch.
- Comparative Visual: show one simple left-right comparison bar titled "先不算 KV，只看权重", with one short red note near the 140 GB block: "单卡 32GB 连权重本体都放不下", and one gray note under the whole section: "这里还没算 runtime buffer / workspace / 框架额外占用".

**Section 2: 显存账之二：128K 的 KV cache 继续往上堆**
- **Mandatory Text Block**: "对 128K 上下文来说，权重是第一座山，KV cache 是第二座山。"
- Show the actual KV accumulation path: the same "128K 上下文" strip is sliced into repeated token cells, each token cell sends two arrows into a per-layer KV drawer labeled "K" and "V", then the drawers replicate downward into a tall cabinet labeled "80 层"; place the simplified formula beside the flow: "KV cache = 上下文长度 × 163,840 × 每元素字节数", and attach one small explanation note right below it: "163,840 = 2 × 层数 × KV heads × head dim = 2 × 80 × 8 × 128", plus one short note "2 = K + V".
- Include technical/details: draw one simple 3-step calculation board titled "KV 是怎么算出来的": first box "单个 token: 163,840", second box "乘以 128K", third box "再乘存储精度", then show three big result boxes only: "fp16 / bf16 ≈ 42.95 GB", "fp8 ≈ 21.47 GB", "4 bit ≈ 10.74 GB"; next to it, draw one easy comparison titled "GQA 帮你省了多少", with two bars only: "不做 GQA：64 个 KV heads ≈ 343.60 GB" and "现在：8 个 KV heads ≈ 42.95 GB", plus one short note "KV cache 降到原来的 1/8".

**Section 3: prefill 计算账**
- **Mandatory Text Block**: "把 128K 一次送进去，模型不是在“读一遍”，而是在 80 层里反复做 full attention。"
- Show the prefill process itself instead of a static result card: the same "128K token 上下文" strip enters as a full strip, then unfolds into one huge dense full attention matrix with both axes labeled "token 位置"; from the matrix, draw three large action arrows labeled "QKᵀ 两两配对" → "softmax" → "乘 V", and place one bold corner badge "O(n²)" with one zoom note "n = 128K".
- Include technical/details: add one compact computation track titled "prefill 为什么贵", showing two parallel tracks that later merge: "投影 + FFN ≈ 1.79 × 10^16 FLOPs" and "Attention core ≈ 2.25 × 10^16 FLOPs", then a final board "合计 ≈ 4.05 × 10^16 FLOPs"; at the far right place one device card "RTX 5090" with "FP16 Tensor 理论峰值约每秒 419 万亿次", connected by one thick orange arrow labeled "理论下界约 96.6 秒".

**Section 4: decode 持续成本**
- **Mandatory Text Block**: "decode 单看 FLOPs 没有 prefill 那么炸，但它不是免费，更不是“上下文一长也无所谓”。"
- Show the decode process as one explicit time-stepped loop: at left keep the old "128K token 上下文" strip and the existing KV cache cabinet in the background, then four large nodes in sequence "读取整段历史 KV" → "只算当前 1 个 token" → "与全部历史做注意力" → "写回新的 K/V"; after that, loop back with one arrow "进入下一个 token", plus one fixed note "KV cache 仍常驻显存".
- Include technical/details: add one result ladder titled "decode 为什么持续付费", showing "单 token FLOPs ≈ 4.80 × 10^11" → "5090 算力下界 ≈ 1.15 ms / token" → "但读 fp16 KV ≈ 42.95 GB / token" → "5090 带宽下界 ≈ 23.99 ms / token"; add one side summary card "生成 1000 个 token" with only three large lines: "总 FLOPs ≈ 4.80 × 10^14", "算力下界 ≈ 1.15 秒", "fp16 KV 带宽下界 ≈ 23.99 秒".

**Style specifications:**
- Explanatory infographic, not decorative illustration
- Hand-drawn technical sketch aesthetic on pure white background
- Single-page vertical engineering layout with strong hierarchy
- Readability first: no tiny packed labels, prefer fewer but more explanatory large labels
- Use the correct visual form for each meaning: parameter anatomy, weight storage comparison, KV storage accumulation, attention matrix, generation loop, final stacked cost summary
- Clean composition with fewer but larger embedded labels
- Prioritize readable section titles, key formulas, and big numbers over micro-annotations
- Functional colors only: navy blue for structure, teal for formulas and arrows, orange for warnings and key conclusions, light gray for guides and background grids
- Keep all embedded labels, notes, axis names, legends, badges, and warning ribbons in Chinese
- Keep English only for technical terms already present in the source, such as DeepSeek-R1-Distill-Llama-70B, dense decoder-only Transformer, hidden size, KV heads, head dim, FLOPs, fp16, bf16, fp8, 4 bit, RTX 5090, GQA, full attention
- Make objects, relationships, transitions, comparisons, formula substitutions, and quantitative evidence visually explicit
