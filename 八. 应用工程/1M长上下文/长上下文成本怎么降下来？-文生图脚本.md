Create a highly detailed infographic with explanatory diagrams, accurate visual semantics, and Chinese labels where text appears inside the image:

**Title (top):**
"成本优化要怎么做？"

**Subtitle (Optional):**
"不是所有优化都在做同一件事：有的先缩活跃上下文，有的改 attention 连接方式，有的避免重复 prefill。"

**Main Content - Detailed sections:**
*Create one section for EACH optimization path in the source text. Keep one continuous mini-case across the full page: the same DeepSeek-R1-Distill-Llama-70B handles the same long strip labeled “128K token 上下文”, and the same long document plus repeated user follow-up questions appear across all sections so the viewer can track how each optimization cuts a different part of the bill.*
*For each section, keep only 1 mandatory text block plus 2 non-redundant visual dimensions. Prefer larger labels and fewer micro-callouts over maximum detail density.*

**Section 1: 先缩活跃上下文**
- **Mandatory Text Block**: "这条路不去碰 attention 公式本身，而是想办法让真正进入公式的 n 先变小。"
- Show one top-down pipeline titled "先筛再算": a full long document wall labeled "128K 原始材料" is first cut into many chunks, then split into three branches with three distinct Chinese labels: "检索裁剪", "摘要压缩", "分层记忆"; after that, the three branches merge into a much shorter active strip labeled "真正进入 attention 的部分".
- Include technical/details: add one compact comparison board titled "活跃上下文一缩，账怎么变", with two vertical columns "压到 64K" and "压到 32K", and three rows only: "fp16 KV cache：42.95 GB → 21.47 GB / 10.74 GB", "prefill：4.05 × 10^16 → 1.46 × 10^16 / 5.89 × 10^15 FLOPs", "decode / token：4.80 × 10^11 → 3.09 × 10^11 / 2.23 × 10^11 FLOPs"; add one orange note "prefill 省得最狠，因为 full attention 主项是 n²".

**Section 2: 改 attention 连接方式**
- **Mandatory Text Block**: "token 还是送进来了，但不是每个 token 都能看到全部历史。"
- Comparative Visual: show one side-by-side attention matrix comparison. On the left, draw a full square matrix labeled "全局 attention" with a full filled grid and one badge "128K × 128K"; on the right, draw two alternatives inside one panel: a diagonal band labeled "滑动窗口" and a sparse block pattern labeled "块稀疏"; add arrows from one current token position to nearby windows, chapter title blocks, and summary blocks, with labels "附近块", "标题块", "摘要块".
- Include technical/details: add one result board titled "把 n² 改薄以后", showing "复杂度更像 n × w", then substitute "n = 131,072, w = 16,000", and end with three large metric cards only: "总 prefill：4.05 × 10^16 → 2.07 × 10^16", "decode / token：4.80 × 10^11 → 1.79 × 10^11", "理想 fp16 热 KV：42.95 GB → 5.24 GB"; add one orange tag "attention core 约省 8.19×，但总账不会等比例下降".

**Section 3: 避免重复 prefill**
- **Mandatory Text Block**: "Prefix cache 解决的不是一段超长上下文本身太贵，而是你为什么还要把同一段超长上下文反复算很多遍。"
- Show one multi-turn timeline titled "同一份长材料被反复追问": first panel "第 1 轮" shows a long prefix strip labeled "120,000 token 前缀" plus a short tail labeled "11,072 token 新增内容", and a big arrow to a cache cabinet labeled "前缀 KV 已建立"; second panel "下一轮" reuses the same prefix cabinet in gray-blue with a bold stamp "直接复用", while only the short new tail is highlighted in orange and sent into compute.
- Include technical/details: add one savings ladder titled "重复 prefill 怎么被省掉", showing "整段重算：4.05 × 10^16 FLOPs" versus "只补新增：5.16 × 10^15 FLOPs", ending in one big green badge "约省 7.8×"; next to it, place three small checklist cards labeled "稳定指纹", "局部失效", "共享边界", with short Chinese notes "前缀完全一致才复用", "中间改 1 个 token 后面就失效", "多租户不是所有 cache 都能共用".

**Style specifications:**
- Explanatory infographic, not decorative illustration
- Choose the right visual form based on meaning
- Clean layout, clear hierarchy, dense but readable
- Readability first: fewer but larger labels when the content is numerically dense
- Functional colors with restrained palette
- Arrows, labels, legends, zoom-ins, guides, and callouts where useful
- Keep text embedded inside the visual explanation
- Make objects, relationships, transitions, comparisons, and evidence visible
