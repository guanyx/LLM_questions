# 《Hermes 是怎么把一次请求解析成 Runtime Provider 的》第 1 张封面文生图脚本

Create a striking premium editorial cover image for social media (Xiaohongshu) on a pure white background. The theme is "Hermes 是怎么把一次请求解析成 Runtime Provider 的". The result must look like a high-end technical magazine cover or a protocol dossier poster, not like a doodle sketch, not like a classroom whiteboard, and not like a generic engineering flowchart.

**Design Concept:**
Use one bold hero object: a large, elegant `Runtime Provider` plate as the final resolved runtime artifact. On the left, a small cluster of messy runtime inputs is being compressed into that final object. The composition should feel premium, sharp, and editorial: large typography, strong negative space, one dominant hero silhouette, very few annotations, and no visual clutter. Hermes should be only a subtle reference, not the protagonist of the image.

**Layout Structure:**

**Top Section (Title Area - 30%):**

- Main title in bold, oversized font, aligned slightly left of center:
  - "Hermes 是怎么把一次请求"
  - "解析成 Runtime Provider 的"
- Title should be split into two lines with deliberate hierarchy, large breathing room, and strong editorial confidence
- Subtitle below in smaller but still prominent text:
  - "Provider 层真正产出的不是模型名，而是一份运行时决定"
- Do not add a third explanatory sentence under the subtitle
- The title area should feel premium and spacious, not dense

**Central Section (Hero Composition - 58%):**
Create one powerful asymmetrical composition with a single dominant hero object:

- Place one large `Runtime Provider` object plate in the center-right area
  - this is the absolute visual center
  - render it like a refined technical specimen plate / runtime dossier slab
  - use crisp geometry, elegant panel divisions, subtle depth, and premium line weight variation
  - the object should feel expensive, precise, and intentional
  - avoid cute sketchiness, avoid messy doodle texture, avoid classroom diagram feeling

- Inside the `Runtime Provider` plate, show only 4 premium compartments:
  - endpoint
  - api_key
  - api_mode
  - recovery path
  - each field should feel like a resolved system property, not a toy label
  - use very short Chinese support labels only:
    - 最终连接地址
    - 最终凭证语义
    - 最终协议模式
    - 最终恢复路径

- On the left side create a smaller, visually compressed cluster of runtime inputs
  - request
  - config.yaml
  - base_url
  - env vars
  - fallback
  - this cluster should look slightly chaotic but still refined
  - do not render these as five big equal cards
  - instead, render them as staggered slips / tabs / thin dossier fragments
  - keep labels extremely short and understated

- Use one elegant, bold compression gesture from left to right
  - not a childish thick arrow
  - more like a directional force band / tapered routing beam / compression stream
  - it should visually communicate "mixed inputs being resolved into one artifact"
  - place only three tiny stage words near this compression gesture:
    - resolve
    - identify
    - bind

- Add a very small annotation near the hero object, not as a slogan block:
  - "不是 provider 字符串"
  - "而是运行时结果"
  - keep this subtle and premium

- On the far right or lower-right edge, add only a faint stability echo:
  - stable
  - ready
  - resolved
  - these should be extremely subtle, almost like metadata, not another composition area

- Add one tiny Hermes marker, almost like a file tab:
  - `Hermes`
  - `public sample`
  - this should be secondary and visually quiet

- At the very top edge of the hero zone, add an understated dependency band:
  - Agent Loop
  - Tools
  - Session
  - Gateway
  - use hairline dependency links pointing toward the `Runtime Provider`
  - these should read as surrounding system context, not as the main picture

**Bottom Section (Minimal Footer - 12%):**

- No bottom guide strip
- No stage timeline
- No repeated explanatory summary
- Keep the bottom mostly empty with premium whitespace
- Add only one tiny footer mark:
  - "Runtime Dossier / 01"

**Visual Priorities:**

- One huge hero object, not many small objects
- Big typography, not too many labels
- Strong asymmetry, not textbook symmetry
- Premium editorial spacing, not crowded technical worksheet spacing
- Controlled annotations, not annotation overload

**What Must Be Clear at a Glance:**

- This cover is about one request becoming one `Runtime Provider`
- The central hero is the resolved runtime object
- The left cluster is mixed runtime input, not provider ranking
- Hermes is only a sample context, not the visual hero
- The cover is about resolution and binding, not model preference

**What To Avoid:**

- Childish doodle feeling
- Naive hand-drawn classroom aesthetic
- Too many tiny boxes of equal importance
- Over-explained technical labels everywhere
- Logo wall or vendor collage
- Generic blue tech dashboard
- Industrial machinery metaphors
- Symmetrical beginner infographic layout

**Visual Style:**

- Premium editorial technical illustration
- White-background protocol poster
- Object Anatomy style, but refined and mature
- Clean precision linework with subtle sketch softness only at the edges
- Strong composition design first, technical detail second
- The cover should look expensive, sharp, and intentional
- Composition ratio:
  - 50% hero object
  - 12% input cluster and directional gesture
  - 13% title typography
  - 25% whitespace

**Color Palette:**

- Deep navy (#223042) for title, key outlines, and primary structure
- Cobalt blue (#2F6BFF) for routing/compression emphasis
- Soft teal (#1AA39A) for stable resolved-state highlights
- Warm orange (#F08A24) for a few ambiguity or input-fragment accents only
- Cool gray (#B8C2CC) for metadata, dividers, and quiet structural lines
- Pure white background

**Typography:**

- All descriptive annotations in simplified Chinese
- Keep these technical terms in English:
  - Runtime Provider
  - endpoint
  - api_key
  - api_mode
  - recovery path
  - Agent Loop
  - Gateway
  - Session
  - Tools
- Typography should feel editorial and premium, not UI-like
- Title should dominate the page with high contrast and serious visual weight
- Annotation text should be sparse and fine, not oversized and busy

**Overall Feel:**

- Premium
- Sharp
- Mature
- Technical but beautiful
- Immediately scroll-stopping
- More like a high-end system poster than a didactic infographic
- Social-media optimized for Xiaohongshu cover, but with magazine-grade taste

Style: Premium editorial technical cover, refined Object Anatomy, runtime dossier hero object, asymmetrical composition, pure white background, large typography, sparse annotations, high-end visual taste
