# 《为什么 2026 的 Agent 必须有 Provider Router》第 1 张封面文生图脚本

Create an eye-catching technical cover infographic for social media in hand-drawn sketch style on pure white background. The theme is "为什么 2026 的 Agent 必须有 Provider Router". The image should feel like a runtime dossier cover rather than a marketing poster.

**Design Concept:**
Use a central runtime-boundary metaphor instead of a brand-comparison poster or industrial machine metaphor. The image should show that a fragmented provider world on the left is being compressed by one central Provider Router into a stable Runtime Provider result on the right. Hermes should appear only as a small sample badge, not as the visual center. The cover must immediately communicate one judgment: the real problem in 2026 is no longer choosing one best model, but absorbing a fragmented multi-provider world into one stable execution boundary.

**Layout Structure:**

**Top Section (Title Area - 24%):**

- Main title in bold, large font: "为什么 2026 的 Agent 必须有 Provider Router"
- Subtitle below: "借 Hermes 看一层正在变成标配的运行时边界"
- One small lead sentence under subtitle:
  - "今天的 Agent 不是缺模型，而是缺一层能把多模型世界压成稳定运行时的边界"
- Typography should feel like a technical magazine cover with strong hierarchy, centered layout, and high readability on mobile

**Central Section (Hero Runtime Diagram - 66%):**
Create a powerful left-to-center-to-right runtime compression diagram with high information density. Because there is no bottom interpretation strip, the hero structure should occupy more vertical space and visually sink lower into the canvas:

- Place one dominant central object board labeled `Provider Router`
  - this is the visual center of gravity
  - render it like a protocol dossier / runtime control plate
  - the board should have inner compartments, not one empty box

- On the left side create a stacked "fragmented provider world" input field with 5 incoming source cards:
  1. OpenAI
  2. Anthropic
  3. OpenRouter
  4. Custom Gateway
  5. Local Model
  - each card should look slightly different in protocol personality
  - add tiny annotation tags around them:
    - endpoint 不同
    - api_mode 不同
    - credentials 不同
    - 成本/能力不同
    - 稳定性不同
  - use converging arrows from these cards into the central Provider Router board
  - important: do not make this look like logo collection art; it should look like heterogeneous runtime inputs

- Inside the central `Provider Router` board show 4 tightly organized sub-panels:
  - `Requested Source`
    - tiny notes:
      - 显式参数
      - config.yaml
      - env vars
      - auto
  - `Provider Identity`
    - tiny notes:
      - 系统内部认谁
      - 不让兼容路径互相污染
  - `ProviderDef`
    - tiny notes:
      - 静态定义
      - 协议轮廓
      - 默认认证方式
    - this panel must visually read as static reference card, not runtime result
  - `Recovery Policy`
    - tiny notes:
      - 当前 provider 内恢复
      - key rotation
      - cross-provider fallback
      - return to primary

- On the right side place one clean resolved output panel labeled `Runtime Provider`
  - make it look stable, compressed, executable
  - show 4 output fields clearly:
    - endpoint
    - api_key
    - api_mode
    - fallback path
  - add one green annotation:
    - "上层依赖的不是 provider 品牌，而是稳定运行时结果"

- Above the central board create a thin top dependency band labeled `Upper-Layer Dependencies`
  - place 4 small dependent modules:
    - Agent Loop
    - Advisor Pattern
    - Gateway
    - ACP
  - connect these modules to the central Router with thin dependency lines
  - every line should be visually annotated as:
    - dependency
  - important: these are dependency relationships, not sequential call arrows

- Add one small Hermes sample tag near the lower-right corner of the central board:
  - `Hermes as public sample`
  - smaller than all main structural labels
  - this should communicate that Hermes is the observation window, not the universal center of the whole picture
  - place this tag close to the lower-right edge of the hero board, not floating at the page bottom

- Add two side callouts:
  - one blue callout labeled `为什么现在必须有`
    - annotation:
      - 多模型分层
      - 多协议并存
      - provider 绑定变成产品缺陷
  - one orange callout labeled `没有这层会怎样`
    - annotation:
      - endpoint / key 串线
      - 协议复杂度上泄
      - fallback 逻辑四处复制

- Add one strong main flow arrow across the hero visual:
  - fragmented providers → Provider Router → Runtime Provider
- Add one tiny interpretation note beside the main arrow:
  - "不是选品牌，而是压缩运行时复杂度"

- Do not create a bottom interpretation strip, stage timeline, or repeated reading guide
- The lower area should stay visually clean, but not empty to the point of imbalance
- Let the hero composition extend slightly downward so the page still feels centered after removing the bottom guide
- Reserve only a thin bottom margin for whitespace and one very small footer label:
  - "为什么 2026 的 Agent 必须有 Provider Router / 01"

**Key Visual Elements:**

- Central runtime dossier board, not a generic flowchart box
- Heterogeneous provider input cards with protocol differences
- One explicit resolved `Runtime Provider` output plate
- Dependency band above, clearly marked as dependency rather than call sequence
- Static-reference treatment for `ProviderDef`
- Small Hermes sample badge only
- Strong compression feeling from left complexity to right stability
- No bottom guide strip or repeated conclusion timeline

**What Must Be Clear at a Glance:**

- The central object is `Provider Router`, not Hermes
- The left side represents fragmented external reality, not a vendor ranking
- The right side represents executable runtime results, not just a provider name
- `ProviderDef` is static definition, not runtime object output
- Upper modules depend on this boundary; they do not manually absorb provider differences themselves

**Visual Style:**

- Hand-drawn technical sketch aesthetic
- Object Anatomy / Runtime Dossier style
- White background with engineering-document clarity
- Dense annotations, but strict hierarchy
- Clean protocol-panel feeling rather than abstract art
- Avoid industrial machinery, gears, server racks, glowing sci-fi UI, or dashboard clichés
- Composition ratio:
  - 60% detailed schematic illustration
  - 15% technical labels and callouts
  - 25% whitespace

**Color Palette:**

- Deep navy (#2C3E50) for outlines, titles, panel frames, and primary text
- Bright blue (#3498DB) for main routing arrows, dependency lines, and central emphasis
- Green (#16A085) for stable runtime output and recovery-success cues
- Orange (#E67E22) for risk, fragmentation, and failure-propagation warnings
- Light gray for helper grids, secondary dividers, and subtle structure lines
- Pure white background

**Typography:**

- All descriptive annotations in simplified Chinese
- Keep these technical terms in English:
  - Provider Router
  - Provider Identity
  - ProviderDef
  - Runtime Provider
  - Agent Loop
  - Advisor Pattern
  - Gateway
  - ACP
  - endpoint
  - api_mode
  - fallback
- Title font should be bold and highly legible on mobile
- Annotation text should be around 10% larger than baseline social infographic labels

**Overall Feel:**

- Hardcore but readable
- Technical and professional
- Immediately understandable as a cover image
- Closer to a protocol teardown poster than a product advertisement
- Social-media optimized for Xiaohongshu carousel cover

Style: Detailed technical hand-drawn cover infographic, Object Anatomy runtime dossier, fragmented-provider-to-stable-runtime compression metaphor, pure white background, rich engineering annotations
